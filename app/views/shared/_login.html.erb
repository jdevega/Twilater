<% if current_user %>
  Welcome <%= current_user.name %>! <%= avatar_for(current_user) %> <%= link_to "Sign Out", signout_path, id: "signout" %>
<% else %>
  <%= link_to "Sign in with Twitter", "/auth/twitter" %>
<% end %>